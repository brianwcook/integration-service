---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: testsubjectconstructors.integration.konflux-ci.dev
spec:
  group: integration.konflux-ci.dev
  names:
    kind: TestSubjectConstructor
    listKind: TestSubjectConstructorList
    plural: testsubjectconstructors
    shortNames:
    - tsc
    singular: testsubjectconstructor
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - jsonPath: .status.lastProcessedResource.name
      name: Last Processed
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TestSubjectConstructor is the Schema for the testsubjectconstructors
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TestSubjectConstructorSpec defines the desired state of TestSubjectConstructor
            properties:
              extractor:
                description: Extractor defines how to extract data from the triggering
                  resource
                properties:
                  image_digest:
                    description: ImageDigest is the JQ expression to extract the image
                      digest
                    type: string
                  image_url:
                    description: ImageURL is the JQ expression to extract the image
                      URL
                    type: string
                  name:
                    description: Name is the JQ expression to extract the component
                      name
                    type: string
                  source:
                    description: Source defines how to extract source information
                    properties:
                      git:
                        description: Git defines how to extract git source information
                        properties:
                          context:
                            description: Context is the JQ expression to extract the
                              git context
                            type: string
                          revision:
                            description: Revision is the JQ expression to extract
                              the git revision
                            type: string
                          url:
                            description: URL is the JQ expression to extract the git
                              URL
                            type: string
                        type: object
                    type: object
                type: object
              selector:
                description: Selector defines which resources should trigger TestSubject
                  construction
                properties:
                  fields:
                    description: Fields defines field-based selection criteria
                    properties:
                      match:
                        additionalProperties:
                          type: string
                        description: Match defines the resource fields to match
                        type: object
                    type: object
                  labels:
                    description: Labels defines label-based selection criteria
                    properties:
                      match:
                        additionalProperties:
                          type: string
                        description: Match defines the labels to match
                        type: object
                    type: object
                type: object
              template:
                description: Template defines the template for the TestSubject to
                  be created
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations defines annotations to apply to the created
                      TestSubject
                    type: object
                  labels:
                    additionalProperties:
                      type: string
                    description: Labels defines labels to apply to the created TestSubject
                    type: object
                type: object
            required:
            - extractor
            - selector
            type: object
          status:
            description: TestSubjectConstructorStatus defines the observed state of
              TestSubjectConstructor
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the TestSubjectConstructor state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastProcessedResource:
                description: LastProcessedResource contains information about the
                  last processed resource
                properties:
                  apiVersion:
                    description: APIVersion is the API version of the resource
                    type: string
                  createdTestSubject:
                    description: CreatedTestSubject is the name of the TestSubject
                      created from this resource
                    type: string
                  kind:
                    description: Kind is the kind of the resource
                    type: string
                  name:
                    description: Name is the name of the resource
                    type: string
                  namespace:
                    description: Namespace is the namespace of the resource
                    type: string
                  processedAt:
                    description: ProcessedAt is when the resource was processed
                    format: date-time
                    type: string
                required:
                - apiVersion
                - kind
                - name
                - namespace
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
